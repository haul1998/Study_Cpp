22강. 키 입력, 도트 그래픽

강의 내용
 - 실시간 키입력 처리를 하기 위해서 GetAsyncKeyState() 함수를 시용한다.
   위의 함수에 사용되는 값은 알파벳 키는 작은 따옴표 (' ')를 사용하여 전달하고
   그 외의 키는 VK로 시작하는 디파인으로 정의된 값을들 사용한다.
   ex) GetAsyncKeyState('R'), GetAsyncKeyState(VK_UP)
 - 도트 그래픽이란 픽셀 단위로 점을 찍어서 표현하는 그래픽을 말한다.

강의 과제
 - 교육자료에 있는 예제를 따라서 한번 만들어 보기